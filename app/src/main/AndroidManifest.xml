<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sapa"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SAPA"
        android:usesCleartextTraffic="true"
    tools:targetApi="31">

    <activity
        android:name=".LoginActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

        <activity android:name=".RegisterActivity" />
        <activity android:name=".CoordinatorDashboardActivity" />
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".CoodinatorAddSchoolActivity" />
        <activity android:name=".CoordinatorAddStudentActivity" />
        <activity android:name=".AdminHomePendingSchoolsActivity" />
        <activity android:name=".AdminHomePendingCoordinatorsActivity" />
        <activity android:name=".AdminAddHospitalActivity" />
        <activity android:name=".AdminAddHospitalBillingAllocationActivity" />
        <activity android:name=".CoordinatorAppointmentActivity" />
        <activity android:name=".CoordinatorAllocationActivity" />
</application>

    </manifest>
